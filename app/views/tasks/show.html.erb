<h1><%= t(".title") %></h1>

<p>
  <strong><%= t("activerecord.attributes.task.title") %></strong>
  <%= @task.title %>
</p>

<p>
  <strong><%= t("activerecord.attributes.task.content") %></strong>
  <%= @task.content %>
</p>

<p>
  <strong><%= t("activerecord.attributes.common.created_at") %></strong>
  <%= @task.created_at %>
</p>

<p>
  <strong><%= t("activerecord.attributes.task.deadline_on") %></strong>
  <%= @task.deadline_on %>
</p>

<p>
  <strong><%= t("activerecord.attributes.task.priority") %></strong>
  <%= t("enums.task.priority.#{@task.priority}") %>
</p>

<p>
  <strong><%= t("activerecord.attributes.task.status") %></strong>
  <%= t("enums.task.status.#{@task.status}") %>
</p>

<%= link_to t("helpers.edit"), edit_task_path(@task), id: "edit-task" %> |
<%= link_to t("helpers.back"), tasks_path, id: "back" %>
